package com.gfg.arrays;

public class Array_Assignment15 {

    /*
     * 1. Segregrate Positve and Negative Numbers in the array.
     *
     * 2. Traverse all the Positive Numbers, i) if, the number is larger then the
     * input array size, do nothing ii) else, the number is within input array size
     * limit, consider number as array index, and mark the element at that index
     * Negative.
     *
     * 3. After doing the above operation, Traverse the array with initial Positve
     * values again, and store the first occurunce of index, whose value is
     * Non-Negative. (Because this means that index value was not present in the
     * array)
     *
     * 4. This stored element is the Result
     */

    static void missingNumber(int arr[], int size) {

        //1st step
        //seggregate positive and negative numbers in the array


    }

    public static void main(String[] args) {
        // TODO Auto-generated method stub


    }

}
